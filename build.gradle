plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.9.23' apply false
}

group = 'icu.takeneko'
version = '1.0'

subprojects {
    version(rootProject.version)
    repositories {
        mavenCentral()
    }
}
